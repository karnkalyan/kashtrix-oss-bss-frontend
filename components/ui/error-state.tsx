import {AlertTriangle,RotateCcw} from "lucide-react";import {Button} from "@/components/ui/button";
export function ErrorState({title="Something went wrong",description,retry}:{title?:string;description:string;retry?:()=>void}){return <div data-ui="error-state" role="alert" className="rounded-xl border border-[var(--status-danger)]/25 bg-[var(--status-danger-bg)] p-5 text-[var(--status-danger)]"><div className="flex gap-3"><span className="rounded-lg bg-card/60 p-2"><AlertTriangle className="size-5 shrink-0"/></span><div><h3 className="font-heading text-sm font-semibold">{title}</h3><p className="mt-1 text-sm leading-relaxed opacity-90">{description}</p>{retry&&<Button size="sm" variant="outline" className="mt-3 border-current/25 bg-card/60" onClick={retry}><RotateCcw/>Retry</Button>}</div></div></div>}
